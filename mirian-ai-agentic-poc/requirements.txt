# Core
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0

# LLM backends — install at least one
anthropic>=0.30,<1.0        # Claude (default)
openai>=1.35,<2.0           # GPT-4o-mini (set LLM_PROVIDER=openai in .env)

# Testing
pytest>=8.2,<9.0
pytest-cov>=5.0,<6.0

# Development
ruff>=0.5,<1.0              # Linting + formatting
mypy>=1.10,<2.0             # Static type checking
